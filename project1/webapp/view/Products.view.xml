<mvc:View
  controllerName="project1.controller.Products"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m">

  <Page
    id="productsPage"
    title="Products"
    showNavButton="true"
    navButtonPress=".onNavBack">
    <subHeader>
      <Toolbar>
        <SearchField
          id="productSearch"
          width="50%"
          placeholder="Search Products..."
          liveChange=".onSearch"/>
      </Toolbar>
    </subHeader>

    <content>
      <Table
        id="productsTable"
        items="{
        path: '/Products',
        parameters: {
        expand: 'Category, Supplier'  }
      }">
        <headerToolbar>
          <Toolbar>
            <Title text="Product List"/>
          </Toolbar>
        </headerToolbar>

        <columns>
          <Column><Text text="Product ID"/></Column>
          <Column><Text text="Name"/></Column>
          <Column><Text text="Category"/></Column>
          <Column><Text text="Supplier"/></Column>
        </columns>

        <items>
          <ColumnListItem>
            <cells>
              <Text text="{ProductID}"/>
              <Link text="{ProductName}" press=".onProductPress"/>
              <Link text="{Category/CategoryName}" press=".onCategoryPress"/>
              <Link text="{Supplier/CompanyName}" press=".onSupplierPress"/>
            </cells>
          </ColumnListItem>
        </items>
      </Table>
    </content>
  </Page>
</mvc:View>
